/**
 * llheadtail – loads the data into a linked list for which we are managing both a pointer to the head and tail
 * of the list, as we discussed in the notes.
 */
#include <stdio.h>

/**
 * Program mainline
 */
int main(int argc, char **argv)
{
	printf("To Do ....\n");
	return 1;
}
